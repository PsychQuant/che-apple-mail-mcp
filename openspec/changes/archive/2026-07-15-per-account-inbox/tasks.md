# Tasks — per-account-inbox

- [x] 1.1 RED：per-account list 含 inbox（末位）+ script 枚舉 `every mailbox of inbox` + resolve 第 5 名映射 `inbox` key + 空值省略 — 落實 MODIFIED requirement 的 inbox 條款
- [x] 1.2 GREEN：`perAccountSpecialMailboxes` append `("inbox", "inbox")` + docs/description 同步；舊 deferral 測試依 #249 契約演進更新
- [x] 2.1 `spectra validate per-account-inbox`；全套件 0 failures
